server.port=8080
server.servlet.context-path=/api
spring.application.name=maven-initializer-backend

# Actuator endpoints
management.endpoints.web.exposure.include=health,info
management.endpoint.health.show-details=when-authorized
